<%= form_with(model: cart_item) do |form| %>
  <% if cart_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cart_item.errors.count, "error") %> prohibited this cart_item from being saved:</h2>

      <ul>
        <% cart_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :listing_id %>
    <%= form.text_field :listing_id %>
  </div>

  <div class="field">
    <%= form.label :cart_num %>
    <%= form.text_field :cart_num %>
  </div>

  <div class="field">
    <%= form.label :delivery_fee_id %>
    <%= form.text_field :delivery_fee_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
